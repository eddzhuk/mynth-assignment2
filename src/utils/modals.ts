export const showProcessModal = (
  status: string,
  title: string,
  message: string
) => {
  // Logic to show process modal
};

export const showSuccessModal = (
  transactionUrl: string,
  addressUrl: string
) => {
  // Logic to show success modal with transaction and address URLs
};
